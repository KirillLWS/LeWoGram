# Сохранение загруженных медиа под уникальным именем, вычисление относительного пути для записи в БД,
# удаление файла при откате транзакции (если делаешь двухфазно), безопасная отдача файла по запросу (с проверкой прав).
