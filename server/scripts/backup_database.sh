#!/usr/bin/env bash
# Ночной бэкап SQLite: копия файла БД в каталог бэкапов с датой в имени, ротация старых копий.
# Вызов из cron на Raspberry Pi (например 03:15): путь к .db брать из переменной или из того же каталога, что в config.
# Права на выполнение: chmod +x backup_database.sh
